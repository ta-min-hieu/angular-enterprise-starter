{
  "production": true,
  "apiBaseUrl": "${API_BASE_URL}",
  "mediaBaseUrl": "${MEDIA_BASE_URL}",
  "apiBaseUrls": { "base": "${MEDIA_BASE_URL}" },
  "logLevel": "${LOG_LEVEL}",
  "authRedirectPath": "${AUTH_REDIRECT_PATH}",
  "forbiddenPath": "${FORBIDDEN_PATH}",
  "features": {}
}
